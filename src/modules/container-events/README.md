# Container Events module

Core types and tools to store and query canonicalized events.

Structure:
- domain/: canonical types
- application/: commands & queries
- infrastructure/: persistence and mappers

This is a lightweight skeleton. Flesh out mappers and schemas per carrier payloads.
