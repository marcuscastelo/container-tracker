# Status Derivation module

Pure, deterministic functions to derive container/process status from timelines.

Structure:
- domain/: canonical derived types
- application/: pure derivation functions
- infrastructure/: persistence adapters (optional)

This is an initial skeleton — implement domain rules and add tests.
