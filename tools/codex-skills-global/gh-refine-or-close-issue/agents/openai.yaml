interface:
  display_name: "Issue Refresh"
  short_description: "Close outdated issues or rewrite them implementation-ready"
  default_prompt: "Use $gh-refine-or-close-issue to compare GitHub issue #45 with the current repo, close it if outdated, or rewrite the issue body so it is implementation-ready."
